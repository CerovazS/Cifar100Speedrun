# Housekeeping Check — 2026-07-03 16:16:25 CEST

## Scope

Periodic repository hygiene check for the CIFAR-100 speedrun workspace. No experiments were launched, no Flywheel or Linear state was mutated, and no source code was edited.

## Commands Run

```bash
rg --files -g 'program.md' -g 'program/**' -g 'orchestration/**' -g 'outputs/**' -g '*.out' -g '*.err' -g 'slurm*' -g '.env' -g '.flywheel.json' -g 'AGENTS.md' -g 'CLAUDE.md' -g 'DEV.md' -g 'CLUSTER.md'
sed -n '1,260p' program.md
for f in program/*.md; do sed -n '1,240p' "$f"; done
setopt NULL_GLOB; for f in orchestration/*/summary.md orchestration/*/logger-handoff.md orchestration/*/agent-trace.jsonl orchestration/housekeeper/*.md; do sed -n '1,240p' "$f"; done
find outputs -maxdepth 4 -type f
find outputs -maxdepth 4 -type d
setopt NULL_GLOB; for f in outputs/cifar100_speedrun/smoke_48402049_4a59bcf/*; do sed -n '1,220p' "$f"; done
setopt NULL_GLOB; for f in slurm/*.sh slurm/*.out slurm/*.err logs/* *.out *.err; do sed -n '1,220p' "$f"; done
sed -n '1,200p' .env
sed -n '1,260p' /Users/lucacerovaz/projects/agent-config/codex/SLURM.md
git status --short
find orchestration/g2-interactive-smoke -maxdepth 2 -type f | sort
sed -n '1,260p' env_setup.sh
sed -n '1,260p' SMOKE_RESULT.md
sed -n '1,260p' BASELINE_PROBES.md
```

## Files Inspected

- `program.md`
- `program/00-adversarial-framing.md`
- `program/01-literature-plan.md`
- `program/02-repo-control.md`
- `program/03-assumption-breaker.md`
- `program/04-execution-dag.md`
- `program/05-plan-audit.md`
- `orchestration/intake/summary.md`
- `orchestration/intake/agent-trace.jsonl`
- `orchestration/implementation-control-patch/summary.md`
- `orchestration/g2-interactive-smoke/summary.md`
- `orchestration/g2-interactive-smoke/srun_20260703_155006.log`
- `orchestration/g2-interactive-smoke/srun_20260703_155127_nopty.log`
- `orchestration/g2-interactive-smoke/srun_20260703_155251_chdir.log`
- `orchestration/g2-interactive-smoke/srun_20260703_155942_triton.log`
- `orchestration/housekeeper/2026-07-03-153614-housekeeping.md`
- `orchestration/housekeeper/2026-07-03-155604-housekeeping.md`
- `outputs/cifar100_speedrun/smoke_48402049_4a59bcf/config.json`
- `outputs/cifar100_speedrun/smoke_48402049_4a59bcf/metrics.csv`
- `outputs/cifar100_speedrun/smoke_48402049_4a59bcf/repro_metadata.json`
- `outputs/cifar100_speedrun/smoke_48402049_4a59bcf/summary.json`
- `outputs/cifar100_speedrun/smoke_48402049_4a59bcf/warmup.json`
- `slurm/smoke.sh`
- `slurm/discovery.sh`
- `slurm/official_baseline.sh`
- `slurm/paired_compare.sh`
- `env_setup.sh`
- `SMOKE_RESULT.md`
- `BASELINE_PROBES.md`
- `.env`
- `/Users/lucacerovaz/projects/agent-config/codex/SLURM.md`

## Findings

1. `program.md` remains current and aligned with the active G3 baseline gate, including the required checklist and explicit references for claim, hypothesis, decision criterion, and evidence.
2. The Leonardo workflow is still aligned with the interactive-before-batch rule and `IscrC_SIMP` account guard in the SLURM wrappers.
3. A unique local smoke run directory now exists at `outputs/cifar100_speedrun/smoke_48402049_4a59bcf`, so the earlier “no outputs tree” gap is closed for G2.
4. That smoke directory still does not meet the full artifact standard expected before durable publication or easy audit: no human-readable Markdown run summary, no visual artifact, no `reproducibility.md`, no `commit.txt`, and no copied scheduler stdout/stderr alongside the metrics JSON/CSV files.
5. `orchestration/intake/agent-trace.jsonl` is stale. The old `Reader`, `PaperScout`, `Scholar`, and one `Planner` entries remain `pending` with empty commands and outputs, even though the literature plan and trajectory outputs now exist elsewhere in the workspace.
6. No `logger-handoff.md` exists for G2 interactive smoke or for the control-patch phase, so a future logging agent would still need to reconstruct context from chat plus multiple summaries.
7. The copied smoke `repro_metadata.json` records `HF_HOME`, `HF_HUB_CACHE`, and `TRANSFORMERS_CACHE` under `/leonardo_scratch/fast/IscrC_YENDRI/...` while the run was billed to `IscrC_SIMP`. This does not prove a rule violation by itself, but it is an account/storage ambiguity that should be resolved before treating the pipeline as cleanly aligned with the SIMP contract.
8. `git status --short` still shows untracked `orchestration/campaign-config/cifar100-campaign-config.json`, so part of the orchestration state is not yet anchored in the repository worktree.

## Blockers

- G2 smoke evidence is locally present but still incomplete for logger handoff or durable audit because the run directory lacks the full summary/reproducibility/commit/log bundle.
- Intake orchestration trace bookkeeping is stale, which weakens the local handoff layer for future logger or critic agents.
- The HF cache/account path ambiguity in the copied smoke metadata remains unresolved.

## Recommended Next Housekeeping Actions

1. Before or immediately after G3, ensure each run directory carries copied scheduler logs plus the missing summary/reproducibility/commit artifacts needed by the local standard.
2. Reconcile `orchestration/intake/agent-trace.jsonl` so obsolete pending entries are closed and completed outputs are reflected once those agents are considered done.
3. Add `orchestration/g2-interactive-smoke/logger-handoff.md` once the smoke is intended to support later Flywheel logging or critic review.
4. Confirm whether shared HF caches under `IscrC_YENDRI` are intentionally allowed for this SIMP-billed workflow; if yes, record that exception explicitly in the local trace to avoid repeated ambiguity.

# Housekeeping Check — 2026-07-03 16:56:18 CEST

## Scope

Periodic repository hygiene check for the CIFAR-100 speedrun workspace. No experiments were launched, no Flywheel or Linear state was mutated, and no source code was edited.

## Commands Run

```bash
sed -n '1,220p' "$CODEX_HOME/automations/cifar100-speedrun-housekeeper-20m/memory.md"
rg --files -g 'program.md' -g 'program/**' -g 'orchestration/**' -g 'outputs/**' -g '*.md' -g '*.out' -g '*.err' -g 'slurm*' -g '.env' -g '.flywheel.json'
sed -n '1,240p' program.md
sed -n '1,220p' orchestration/housekeeper/2026-07-03-163628-housekeeping.md
sed -n '1,220p' orchestration/g5-search/assignments.md
sed -n '1,220p' orchestration/g5-t1-schedule/summary.md
sed -n '1,220p' orchestration/g5-t2-muon/summary.md
sed -n '1,220p' orchestration/g5-t3-arch/summary.md
sed -n '1,220p' .env
sed -n '1,220p' /Users/lucacerovaz/projects/agent-config/codex/SLURM.md
find outputs -maxdepth 3 -type d | sort
find outputs -maxdepth 3 -type f | sort
find orchestration -maxdepth 3 -type f | sort
git status --short
sed -n '1,260p' orchestration/g4-paired-pilot/summary.md
sed -n '1,260p' orchestration/intake/agent-trace.jsonl
sed -n '1,260p' orchestration/g5-t1-schedule/agent-trace.jsonl
sed -n '1,260p' orchestration/g5-t2-muon/agent-trace.jsonl
sed -n '1,260p' orchestration/g5-t3-arch/agent-trace.jsonl
for d in outputs/cifar100_speedrun/*; do find "$d" -maxdepth 2 -type f | sort; done
find orchestration -maxdepth 3 -name 'logger-handoff.md' -print | sort
sed -n '1,220p' orchestration/g3-official-baseline/summary.md
date '+%Y-%m-%d %H:%M:%S %Z'
```

## Files Inspected

- `program.md`
- `.env`
- `/Users/lucacerovaz/projects/agent-config/codex/SLURM.md`
- `orchestration/housekeeper/2026-07-03-163628-housekeeping.md`
- `orchestration/g3-official-baseline/summary.md`
- `orchestration/g4-paired-pilot/summary.md`
- `orchestration/g5-search/assignments.md`
- `orchestration/g5-t1-schedule/summary.md`
- `orchestration/g5-t1-schedule/agent-trace.jsonl`
- `orchestration/g5-t2-muon/summary.md`
- `orchestration/g5-t2-muon/agent-trace.jsonl`
- `orchestration/g5-t3-arch/summary.md`
- `orchestration/g5-t3-arch/agent-trace.jsonl`
- `orchestration/intake/agent-trace.jsonl`
- `outputs/cifar100_speedrun/baseline_48402781_27eb750/*`
- `outputs/cifar100_speedrun/smoke_48402049_4a59bcf/*`
- `outputs/cifar100_speedrun/paired_noop_g4_20260703_162639_eb4f1a5/*`

## Findings

1. `program.md` still contains the required checklist and the active G5-T1 claim/hypothesis/criterion, but it is slightly stale relative to workspace state: `orchestration/g5-search/assignments.md` shows T1, T2, and T3 are all active, while `program.md` still frames the current phase as T1-only.
2. Output-directory isolation is still healthy locally. The checked run directories are unique, and I saw no local evidence of output reuse or overwrites.
3. Local artifact coverage improved with G4: the paired pilot directory includes paired summaries, per-seed metrics/config/metadata, and copied `stdout.log` / `stderr.log`.
4. The durable artifact bundle is still incomplete for every locally visible run. G2, G3, and G4 local output trees still lack a Markdown run summary, visual artifact, `reproducibility.md`, and `commit.txt`, so they are not logger-ready under the stated local artifact standard.
5. No `orchestration/*/logger-handoff.md` files exist yet. This remains a concrete gap now for at least G3 and G4, and likely for G5 tracks once their pilot outputs need review or publication.
6. Orchestration bookkeeping is stale in multiple traces:
   - `orchestration/intake/agent-trace.jsonl` still contains obsolete `pending` rows for `Reader`, `PaperScout`, `Scholar`, and one `Planner` entry despite later completed literature/planning rows.
   - `orchestration/g5-t1-schedule/agent-trace.jsonl` still has an `in_progress` prelaunch row even though discovery finished and the next step is blocked on QoS.
   - `orchestration/g5-t2-muon/agent-trace.jsonl` still starts with a `pending` planning row after the trace already records one completed pilot and a submitted follow-up job.
7. The active G5 search state is only partially local. The workspace contains local trace summaries, but the actual G5 run outputs referenced in those traces are remote-only, so a future logger or critic cannot complete the audit from the local repository alone.
8. `git status --short` still shows active local modifications and untracked orchestration state (`program.md`, `cifar100-benchmark/train_cifar100_resnet_muon.py`, `orchestration/g5-*`, `orchestration/campaign-config/`). That is not itself a bug, but it means handoff material must clearly distinguish committed baseline evidence from in-flight working state.

## Blockers

- Logger handoff documents are still missing.
- Several orchestration traces have stale statuses that no longer match the recorded summaries.
- The local run artifact bundle is still below the repo's stated audit/logging standard.
- G5 pilot evidence is not mirrored locally yet, so local-only reconstruction remains incomplete.

## Recommended Next Housekeeping Actions

1. Update `program.md` so the active phase reflects the real multi-track G5 state or explicitly says T2/T3 are delegated side tracks under the same gate.
2. Add `logger-handoff.md` files for G3 and G4 before the next logging or critic pass, then extend the same pattern to G5 tracks as results stabilize.
3. Reconcile stale `pending` / `in_progress` rows in the intake and G5 trace JSONL files so status matches the actual completed, blocked, or remote-running state.
4. Ensure future locally curated run bundles include the missing Markdown summary, one visual artifact, `reproducibility.md`, and `commit.txt`, or explicitly document that the evidence remains remote-only pending sync.

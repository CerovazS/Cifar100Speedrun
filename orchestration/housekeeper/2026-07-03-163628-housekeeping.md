# Housekeeping Check — 2026-07-03 16:36:28 CEST

## Scope

Periodic repository hygiene check for the CIFAR-100 speedrun workspace. No experiments were launched, no Flywheel or Linear state was mutated, and no source code was edited.

## Commands Run

```bash
test -f "$CODEX_HOME/automations/cifar100-speedrun-housekeeper-20m/memory.md" && sed -n '1,220p' "$CODEX_HOME/automations/cifar100-speedrun-housekeeper-20m/memory.md"
rg -n "cifar100|speedrun|Cifar100 Speedrun|housekeeper" /Users/lucacerovaz/.codex/memories/MEMORY.md
pwd
rg --files -g 'AGENTS.md' -g 'CLAUDE.md' -g 'DEV.md' -g 'CLUSTER.md' -g '.env' -g '.flywheel.json' -g 'program.md' -g 'index.md' -g 'SLURM.md' .
sed -n '1,240p' program.md
sed -n '1,200p' .env
find . -maxdepth 3 \( -path './.git' -o -path './.venv' \) -prune -o -type d \( -name program -o -name orchestration -o -name outputs -o -name slurm -o -name logs \) -print | sort
find . -maxdepth 2 -type f \( -name '*.out' -o -name '*.err' -o -name 'slurm-*' -o -name '*.log' \) | sort
find program -maxdepth 2 -type f | sort
find orchestration -maxdepth 3 \( -type f -o -type d \) | sort
find outputs -maxdepth 4 \( -type f -o -type d \) | sort
find slurm -maxdepth 4 \( -type f -o -type d \) | sort
sed -n '1,220p' orchestration/intake/summary.md
sed -n '1,220p' orchestration/g2-interactive-smoke/summary.md
sed -n '1,240p' orchestration/g3-official-baseline/summary.md
sed -n '1,220p' orchestration/implementation-control-patch/summary.md
for f in orchestration/housekeeper/*.md; do sed -n '1,220p' "$f"; done
sed -n '1,260p' orchestration/intake/agent-trace.jsonl
for f in outputs/cifar100_speedrun/baseline_48402781_27eb750/*; do sed -n '1,220p' "$f"; done
sed -n '1,260p' program/05-plan-audit.md
find orchestration -maxdepth 3 -name 'logger-handoff.md' -print | sort
git status --short
date '+%Y-%m-%d %H:%M:%S %Z'
sed -n '1,240p' outputs/cifar100_speedrun/smoke_48402049_4a59bcf/repro_metadata.json
find outputs/cifar100_speedrun -maxdepth 2 -type f | sort
```

## Files Inspected

- `program.md`
- `.env`
- `program/00-adversarial-framing.md`
- `program/01-literature-plan.md`
- `program/02-repo-control.md`
- `program/03-assumption-breaker.md`
- `program/04-execution-dag.md`
- `program/05-plan-audit.md`
- `orchestration/intake/summary.md`
- `orchestration/intake/agent-trace.jsonl`
- `orchestration/g2-interactive-smoke/summary.md`
- `orchestration/g3-official-baseline/summary.md`
- `orchestration/implementation-control-patch/summary.md`
- `orchestration/housekeeper/2026-07-03-153614-housekeeping.md`
- `orchestration/housekeeper/2026-07-03-155604-housekeeping.md`
- `orchestration/housekeeper/2026-07-03-161625-housekeeping.md`
- `outputs/cifar100_speedrun/smoke_48402049_4a59bcf/repro_metadata.json`
- `outputs/cifar100_speedrun/baseline_48402781_27eb750/analysis.txt`
- `outputs/cifar100_speedrun/baseline_48402781_27eb750/config.json`
- `outputs/cifar100_speedrun/baseline_48402781_27eb750/metrics.csv`
- `outputs/cifar100_speedrun/baseline_48402781_27eb750/repro_metadata.json`
- `outputs/cifar100_speedrun/baseline_48402781_27eb750/stdout.log`
- `outputs/cifar100_speedrun/baseline_48402781_27eb750/stderr.log`
- `outputs/cifar100_speedrun/baseline_48402781_27eb750/summary.json`
- `outputs/cifar100_speedrun/baseline_48402781_27eb750/warmup.json`
- `slurm/discovery.sh`
- `slurm/official_baseline.sh`
- `slurm/paired_compare.sh`
- `slurm/smoke.sh`
- `/Users/lucacerovaz/projects/agent-config/codex/SLURM.md`

## Findings

1. `program.md` is current, keeps the required checklist visible, and still matches the active G4 paired-pilot gate described in the workspace.
2. Run isolation is currently healthy: `outputs/cifar100_speedrun/smoke_48402049_4a59bcf` and `outputs/cifar100_speedrun/baseline_48402781_27eb750` are unique run directories, and no output-directory reuse was observed.
3. G3 baseline evidence is materially stronger than G2 smoke evidence: the baseline run directory contains machine-readable metrics plus copied `stdout.log` and `stderr.log`, while the smoke run directory still lacks copied scheduler or console logs.
4. Neither run directory currently satisfies the full local artifact bundle expected for durable audit or easy logger handoff. Missing items across the current runs include a human-readable Markdown run summary, at least one visual artifact, `reproducibility.md`, and `commit.txt`.
5. No `orchestration/*/logger-handoff.md` files exist yet. This is now a concrete gap for both `g2-interactive-smoke` and `g3-official-baseline`, because evidence exists and later Flywheel logging would still require reconstruction from multiple summaries and raw files.
6. `orchestration/intake/agent-trace.jsonl` remains stale. Older `Reader`, `PaperScout`, `Scholar`, and one `Planner` rows are still marked `pending` with empty commands and outputs even though newer completed rows and downstream planning artifacts now exist.
7. The recorded Leonardo metadata still shows `HF_HOME`, `HF_HUB_CACHE`, and `TRANSFORMERS_CACHE` under `/leonardo_scratch/fast/IscrC_YENDRI/...` for both the SIMP-billed smoke and baseline runs. This does not prove contract breakage by itself, but it preserves the same SIMP-vs-YENDRI cache ambiguity noted by prior housekeeping passes.
8. `git status --short` still shows untracked `orchestration/campaign-config/cifar100-campaign-config.json`, so one orchestration-state artifact is still not anchored in the worktree.

## Blockers

- Logger handoff material is still missing for the completed G2 and G3 phases.
- Intake orchestration bookkeeping is stale because obsolete `pending` subagent rows were never reconciled.
- Current run directories are usable for local inspection, but not yet complete enough for durable audit or publication-grade logging because the summary/reproducibility/commit/visual bundle is incomplete.
- The HF cache/account-path ambiguity remains unresolved in recorded metadata.

## Recommended Next Housekeeping Actions

1. Before or immediately after the next paired pilot, add `logger-handoff.md` files for G2 and G3 so a logging agent can work from local traces instead of chat reconstruction.
2. Reconcile `orchestration/intake/agent-trace.jsonl` by closing obsolete `pending` entries and recording outputs for the literature/planning work that already landed.
3. Ensure future run directories include the missing local-standard artifacts: Markdown summary, visual evidence, `reproducibility.md`, and `commit.txt`, with smoke logs copied into the run directory the same way G3 already copies baseline logs.
4. Resolve or explicitly document the `IscrC_SIMP` vs `IscrC_YENDRI` HF cache-path exception so later critics and loggers do not have to guess whether it is intentional.
